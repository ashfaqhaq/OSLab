#include<iostream>
main()
	{
		int arr[l];
		int l;
		cout<<"Input array size";
		cin>>l;
		cout<<"input capacity";
		int n;
		cin>>n;
		for (i=0;i<l;i++)
		{
			cin>>arr[i];
		}
		n1=arr[0];
		for(i=1;i<l;i++)
		{
			if (a[i]!=n1 || a[i]!=n2 || a[i]!=n3 || a[i]!=n4)
			{
				fault+=1;
											
